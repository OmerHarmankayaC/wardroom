# Wardroom environment — shape only, never values.
# No variables are required yet (pre-alpha). Once the core exists this file
# will list, at minimum, the Anthropic auth variable for the chosen SDK.
